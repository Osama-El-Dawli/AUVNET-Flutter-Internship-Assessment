import 'package:flutter/material.dart';

class DeliverView extends StatelessWidget {
  const DeliverView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Deliver View'));
  }
}
